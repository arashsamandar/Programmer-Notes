### Putting your variable inside `php` String like Bellow :

```php
$username = "arash";
echo "Hello User {$username}";
```

> so just put the variable in the string with  `{$variable}` sign attached to it . and php detects it as Variable