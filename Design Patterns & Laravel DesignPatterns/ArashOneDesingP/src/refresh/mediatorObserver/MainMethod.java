package refresh.mediatorObserver;

public class MainMethod {
    public static void main(String[] args) {
        var mediator = new MediatorClass();
        mediator.simulateMediatorCaseEmOneChanged();
        // thus the problem is solved
    }
}
